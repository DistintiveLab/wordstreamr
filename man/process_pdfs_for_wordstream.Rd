% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_pdfs_for_wordstream.R
\name{process_pdfs_for_wordstream}
\alias{process_pdfs_for_wordstream}
\title{Processa PDFs, extrai termos e os categoriza opcionalmente usando um LLM}
\usage{
process_pdfs_for_wordstream(
  folder_path,
  language = "pt",
  ngram_range = 1:2,
  blacklist = NULL,
  top_n_words = 100,
  categories = "auto",
  top_n_categories = NULL,
  llm_provider = "ollamar"
)
}
\arguments{
\item{folder_path}{Caminho para a pasta contendo os arquivos PDF.}

\item{language}{Idioma dos documentos para seleção da blacklist e stemming. Padrão "pt".
Valores possíveis: "pt", "en", "es".}

\item{ngram_range}{Um vetor numérico indicando os tamanhos de n-gram a serem extraídos.
Padrão \code{1:2} (palavras únicas e bigramas).}

\item{blacklist}{Uma lista de palavras a serem removidas. Se NULL (padrão), usa uma
blacklist interna para o idioma selecionado.}

\item{top_n_words}{Número máximo de termos a serem retornados, baseado na frequência. Padrão 100.}

\item{categories}{Define o método de categorização. Padrão "auto". Pode ser:
\itemize{
\item \code{"auto"}: Se \code{top_n_categories} for definido, o LLM criará as categorias livremente.
\item \code{"pos"}: O LLM atuará como um tagger de Part-of-Speech.
\item \code{"ner"}: O LLM atuará como uma ferramenta de Named-Entity Recognition.
\item Um vetor de strings (ex: \code{c("Tecnologia", "Finanças")}): Força o LLM a usar
essas categorias temáticas pré-definidas.
}}

\item{top_n_categories}{Número de categorias a serem geradas pelo LLM. Relevante apenas
quando \code{categories = "auto"}. Se NULL (padrão), a categorização por LLM é desativada.}

\item{llm_provider}{O provedor de LLM a ser usado. Padrão "ollamar".
Alternativas: "chatgpt", "claude", etc. (depende do pacote \code{chatLLM}).}
}
\value{
Um data frame (tibble) "tidy" com as colunas \code{date}, \code{term}, \code{frequency} e
\code{category} (se a categorização LLM for ativada). Perfeito para usar com
\code{prepare_wordstream_data}.
}
\description{
Esta função varre uma pasta em busca de arquivos PDF, extrai texto e datas,
processa o texto para encontrar termos relevantes (palavras e n-grams),
e opcionalmente usa um modelo de linguagem grande (LLM) para categorizá-los.
}
